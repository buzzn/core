.modal-dialog
  .modal-content
    .modal-header
      %button.close{"aria-hidden" => "true", "data-dismiss" => "modal", type: "button"} ×
      %h4.modal-title= t'new_servicing_contract'
    .modal-body
      = simple_form_for @servicing_contract, remote: true do |f|
        - value = params[:group_id].nil? ? {} : { value: params[:group_id] }
        = f.input :group_id, input_html: {}.merge(value), as: :hidden
        = render 'servicing_contracts/form_fields', f: f
        .modal-footer
          = f.button :submit, id: 'submit', class: 'btn btn-success'