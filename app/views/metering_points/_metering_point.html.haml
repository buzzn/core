.col-xs-12.col-sm-12.col-md-12.col-lg-12.metering_point{id: "metering_point_#{metering_point.id}", data: {smart: "#{metering_point.smart?}", online: "#{metering_point.online?}"} }

  = link_to metering_point do
    .panel.widget
      - if !metering_point.smart?
        .ribbon-wrapper-green{"data-toggle" => "popover", "data-placement" => "left", "title" => t("#{metering_point.fake_label}_abbreviation"), "data-content" => t("dummy_description")}
          .ribbon-green= t("#{metering_point.fake_label}")
      .widget-header{class: "bg-#{metering_point.mode_class}" }
        .pull-left.readability{class: "fa fa-#{metering_point.readable_icon}", 'data-title' => t('readable_by') + ': ' + t("#{metering_point.readable}")}

        .chart{id: "chart-container-#{metering_point.id}", style: "width: 100%; height: 180px;"}
          .waiting-spinner.fa.fa-spinner.fa-2x.fa-spin

      .widget-body.text-center

        %span.icon-wrap.icon-wrap-sm.icon-circle.widget-img.img-border-light{ class: "bg-#{metering_point.mode_class}"}
          %i.fa.fa-bolt.fa-3x


        %p.text-2x.mar-no= metering_point.long_name
        %ul.list-unstyled.text-center.pad-top.mar-no.clearfix

          = content_tag :div, id: "metering_point-ticker-#{metering_point.id}", class: "metering_point-ticker", data: { fake: !metering_point.smart? && !metering_point.virtual, source: metering_point.data_source } do
            %span.text-4x
              .power-ticker
                = t('loading') + '...'
          %p.text-muted Watt

