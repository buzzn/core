.modal-dialog
  .modal-content
    .modal-header
      %button.close{"aria-hidden" => "true", "data-dismiss" => "modal", type: "button"} ×
      %h4.modal-title= t'new_metering_point'
    .modal-body
      = simple_form_for @metering_point, html: {class: 'form-horizontal' }, wrapper: :horizontal_form do |f|
        - value = params[:location_id].nil? ? {} : { value: params[:location_id] }
        = f.input :location_id, input_html: {}.merge(value), as: :hidden
        = render 'metering_points/form_fields', f: f
        .modal-footer
          = f.button :submit, id: 'submit', class: 'btn btn-success'
