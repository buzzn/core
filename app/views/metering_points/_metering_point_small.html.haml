.col-sm-6.col-md-6.col-lg-3.metering_point{id: "metering_point_#{metering_point.id}" }
  = link_to metering_point do
    .panel.panel-info.panel-colorful.text-center

      .panel-body{class: "bg-#{metering_point.mode_class}" }
        .chart{id: "chart-container-#{metering_point.id}", style: "width: 100%; height: 70px;"}
          = t('loading') + '...'

      .bg-light.pad-ver
        %h4.mar-no.text-thin
          =metering_point.long_name
        - if request.original_url.include?("dashboard")
          .display-in-series

            - if user_signed_in?
              - if current_user.dashboard.dashboard_metering_points.where(metering_point_id: metering_point.id).first.displayed
                = metering_point.remove_from_series
              - else
                = metering_point.display_in_series

          .remove-from-dashboard
            = metering_point.remove_from_dashboard
        - else
          %ul.list-unstyled.text-center.pad-top.mar-no.clearfix

            = content_tag :div, id: "metering_point-ticker-#{metering_point.id}", class: "metering_point-ticker", data: { slp: !metering_point.smart? && !metering_point.virtual } do
              %span.text-2x
                .power-ticker
                  = t('loading') + '...'
            %p.text-muted Watt
