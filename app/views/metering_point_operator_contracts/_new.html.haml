.modal-dialog
  .modal-content
    .modal-header
      %button.close{"aria-hidden" => "true", "data-dismiss" => "modal", type: "button"} ×
      %h4.modal-title= t'new_metering_point_operator_contract'
    .modal-body
      = simple_form_for @metering_point_operator_contract, remote: true, html: {class: 'form-horizontal' }, wrapper: :horizontal_form do |f|
        - value = params[:metering_point_id].nil? ? {} : { value: params[:metering_point_id] }
        = f.input :metering_point_id, input_html: {}.merge(value), as: :hidden
        = f.association :organization, collection: Organization.metering_point_operators, input_html: { class: 'select2'}
        = render 'metering_point_operator_contracts/form_fields', f: f
        = f.button :submit, id: 'submit', class: 'btn btn-success'
