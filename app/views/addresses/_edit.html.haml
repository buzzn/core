.modal-content
  .modal-header
    %button.close{"aria-hidden" => "true", "data-dismiss" => "modal", type: "button"} ×
    %h4.modal-title= t'edit_address'
  .modal-body
    = simple_form_for @address, remote: true do |f|
      = render 'addresses/form_fields', f: f
      .modal-footer
        = f.button :submit, id: 'submit', class: 'btn btn-success'
        = @address.decorate.link_to_delete
