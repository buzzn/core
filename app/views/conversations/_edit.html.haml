.modal-content
  .modal-header
    %button.close{"aria-hidden" => "true", "data-dismiss" => "modal", type: "button"} ×
    %h4.modal-title= t'edit_conversation'

  .modal-body
    = simple_form_for @conversation, remote: true do |f|
      = f.association :users, collection: current_user.friends.registered, input_html: { class: 'select2'}, label: false, include_blank: false
      .modal-footer
        .btn.btn-primary.btn-labeled.fa.fa-remove{"data-dismiss" => "modal"}
          = t'cancel'
        = f.button :button, t('save'), id: 'submit', class: 'btn btn-success btn-labeled fa fa-check'