#content
  .forminput.box
    %h1= t'contracting_party'

    = simple_form_for @contracting_party do |f|

      = f.input :legal_entity, collection: ['ich', 'gmbh'], include_blank: false, input_html: { class: 'select2' }

      %fieldset
        %legend= t'organization'
        = f.simple_fields_for :organization do |o|
          = render 'organizations/form_fields', f: o

      %fieldset
        %legend= t'address'
        = f.simple_fields_for :address do |a|
          = render 'addresses/form_fields', f: a



      = f.submit t("continue"), class: 'btn btn-success', id: 'submit'