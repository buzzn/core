= f.input :logo
= f.input :image
= f.input :name
= f.input :website
= f.input :description
= f.input :readable, collection: Group.readables.map(&:to_sym), include_blank: false
= f.input :mode, as: :radio_buttons, collection: Group.modes.map(&:to_sym), label_method: ->(obj){ t(obj)}
