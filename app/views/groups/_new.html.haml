.modal-content
  .modal-header
    %button.close{"aria-hidden" => "true", "data-dismiss" => "modal", type: "button"} ×
    %h4.modal-title= t'new_group'
  .modal-body
    = simple_form_for @group, remote: true do |f|
      = render 'groups/form_fields', f: f
      = f.association :metering_points, collection: MeteringPoint.editable_by_user(current_user).outputs.without_group, include_blank: false, input_html: { class: 'select2'}
      .modal-footer
        = f.button :submit, id: 'submit', class: 'btn btn-success'


