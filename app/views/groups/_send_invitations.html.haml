.modal-content
  .modal-header
    %button.close{"aria-hidden" => "true", "data-dismiss" => "modal", type: "button"} ×
    %h4.modal-title= t'send_invitations'
  .modal-body
    = simple_form_for @group, remote: true, url: send_invitations_update_group_path, method: 'put' do |f|
      = f.input :new_meters, collection: Meter.all, input_html: { class: 'select2'}, label: false, selected: Meter.all.first, :label_method => lambda { |m| m.manufacturer_product_serialnumber.to_s + ' - ' + m.metering_points.first.managers.first.name.to_s }, include_blank: false
      .modal-footer
        .btn.btn-primary.btn-labeled.fa.fa-remove{"data-dismiss" => "modal"}
          = t'cancel'
        = f.button :button, t('save'), id: 'submit', class: 'btn btn-success btn-labeled fa fa-check'