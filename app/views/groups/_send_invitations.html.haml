.modal-content
  .modal-header
    %button.close{"aria-hidden" => "true", "data-dismiss" => "modal", type: "button"} ×
    %h4.modal-title= t'add_metering_points'
  .modal-body
    = simple_form_for @group, remote: true, url: send_invitations_update_group_path, method: 'put' do |f|
      = f.input :add_own_metering_point, as: :boolean, wrapper: :vertical_boolean, input_html: { checked: false }
      .js-dependent-fields{data: {checkbox: {id: 'group_add_own_metering_point', value: 'true'}}}
        = f.input :metering_point_id, collection: MeteringPoint.editable_by_user(current_user).without_group - @group.metering_points, input_html: { class: 'select2', 'data-placeholder': t('add_metering_point')}, label: false, selected: '', include_blank: true, :label_method => lambda { |m| m.decorate.name }
      .js-dependent-fields{data: {checkbox: {id: 'group_add_own_metering_point', value: 'false'}}}
        %p= t('localpool_invite_metering_points_info')
        = f.input :new_meters, input_html: { value: ''}
      .modal-footer
        .btn.btn-primary.btn-labeled.fa.fa-remove{"data-dismiss" => "modal"}
          = t'cancel'
        = f.button :button, t('confirm'), id: 'submit', class: 'btn btn-success btn-labeled fa fa-check'