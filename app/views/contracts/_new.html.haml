.modal-content
  .modal-header
    %button.close{"aria-hidden" => "true", "data-dismiss" => "modal", type: "button"} ×
    %h4.modal-title= t'new_contract'
  .modal-body
    = simple_form_for @contract, remote: true do |f|

      - value = params[:register_id].nil? ? {} : { value: params[:register_id] }
      = f.input :register_id, input_html: {}.merge(value), as: :hidden
      - value2 = params[:group_id].nil? ? {} : { value: params[:group_id] }
      = f.input :group_id, input_html: {}.merge(value2), as: :hidden

      = render 'contracts/form_fields', f: f
      .modal-footer
        .btn.btn-primary.btn-labeled.fa.fa-remove{"data-dismiss" => "modal"}
          = t'cancel'
        = f.button :button, t('save'), id: 'submit', class: 'btn btn-success btn-labeled fa fa-check'
