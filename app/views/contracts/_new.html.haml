.modal-content
  .modal-header
    %button.close{"aria-hidden" => "true", "data-dismiss" => "modal", type: "button"} ×
    %h4.modal-title= t'new_contract'
  .modal-body
    = simple_form_for @contract, remote: true do |f|

      - value = params[:metering_point_id].nil? ? {} : { value: params[:metering_point_id] }
      = f.input :metering_point_id, input_html: {}.merge(value), as: :hidden
      - value2 = params[:group_id].nil? ? {} : { value: params[:group_id] }
      = f.input :group_id, input_html: {}.merge(value2), as: :hidden

      = render 'contracts/form_fields', f: f
      .modal-footer
        = f.button :submit, id: 'submit', class: 'btn btn-success', :value => t('create_contract')
