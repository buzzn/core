%nav#mainnav-container
  #mainnav
    #mainnav-shortcut
      %ul.list-unstyled

        %li.col-xs-6{"data-content" => t('users')}
          = link_to( content_tag(:i, nil, class: 'fa fa-user'), profiles_path, class: 'shortcut-grid')

        %li.col-xs-6{"data-content" => t('groups')}
          = link_to( content_tag(:i, nil, class: 'fa fa-users'), groups_path, class: 'shortcut-grid')


    #mainnav-menu-wrap
      .nano
        .nano-content
          %ul#mainnav-menu.list-group
            - if user_signed_in?

              %li.list-header{"data-content" => ''}= t'private_area'
              = mainnav_profile_path(current_user.profile)
              = mainnav_conversations_path
              - if !current_user.dashboard.nil?
                = mainnav_dashboard_path(current_user.dashboard)
              / = mainnav_stream_path

              - if current_user.accessible_registers.any?
                %li.list-header= t'my_registers'
                - current_user.accessible_registers_by_address.each do |registers_address|
                  %li{class: is_active_link?(registers_path, :inclusive) ? 'active active-sub' : ''}
                    %a{"data-original-title" => "", :href => "#", :title => ""}
                      %i.fa.fa-bolt
                      %span.menu-title
                        - if registers_address[:address]
                          = truncate( registers_address[:address].street_name + " " + registers_address[:address].street_number + " " + registers_address[:address].city, length: 23)
                        - else
                          = t('without_address')
                      %i.arrow

                    %ul.collapse.out
                      - registers_address[:registers].each do |register|
                        %li
                          = mainnav_register_path(register.decorate)




                - if current_user.accessible_groups.any?
                  %li.list-header= t'my_groups'
                  - current_user.accessible_groups.each do |group|
                    = mainnav_group_path(group)
              / - if current_user.editable_devices.any?
              /   %li.list-header= t'my_devices'
              /   - current_user.editable_devices.each do |device|
              /     = mainnav_device_path(device)

              - if current_user.friends.any?
                %li.list-header= t'my_friends'
                - current_user.friends.each do |user|
                  = mainnav_profile_path(user.profile) if user.profile

            %li.list-header= t'legals'
            = link_to t('legal_info'), page_path('legal_info')
            - if user_signed_in?
              = link_to t('terms_of_use'), page_path('terms_of_use')
              = link_to t('data_protection_guidelines'), page_path('data_protection_guidelines')












