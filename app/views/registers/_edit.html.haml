.modal-content
  .modal-header
    %button.close{"aria-hidden" => "true", "data-dismiss" => "modal", type: "button"} ×
    %h4.modal-title= t'edit_register'
  .modal-body
    = simple_form_for @register, remote: true, url: register_path(@register) do |f|
      = render 'registers/form_fields', f: f, locals: { register: @register}
      .modal-footer
        .float-left= @register.decorate.link_to_delete
        .btn.btn-primary.btn-labeled.fa.fa-remove{"data-dismiss" => "modal"}
          = t'cancel'
        = f.button :button, t('save'), id: 'submit', class: 'btn btn-success btn-labeled fa fa-check'
