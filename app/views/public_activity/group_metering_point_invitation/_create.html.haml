- single_content_for :action do
  - if a.trackable && a.recipient
    = t("has_invited_the_metering_point_to_join_the_group", group_name: link_to(a.trackable.name, a.trackable), metering_point_name: link_to(a.recipient.name, a.recipient)).html_safe
  - else
    = t('no_information_available')