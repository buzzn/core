- single_content_for :action do
  - if a.recipient
    = t('sent_a_friendship_request_to_user', user: link_to(a.recipient.name, profile_path(a.recipient.profile))).html_safe
  - else
    = t('sent_a_friendship_request_to') + ' ' + t('an_unknown_user')