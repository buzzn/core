- single_content_for :action do
  - if a.trackable && a.recipient
    = t("has_appointed_user_group_manager_for_group", user: a.trackable.name, group: a.recipient.name)
  - else
    = t('no_information_available')