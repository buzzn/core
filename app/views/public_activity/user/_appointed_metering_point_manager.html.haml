- single_content_for :action do
  - if a.trackable && a.recipient
    = t("has_appointed_user_metering_point_manager_for_metering_point", user: a.trackable.name, metering_point: a.recipient.name)
  - else
    = t('no_information_available')