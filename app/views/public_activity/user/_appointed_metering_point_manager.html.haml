- single_content_for :action do
  - if a.trackable && a.recipient
    = t("has_appointed_user_register_manager_for_register", user: a.trackable.name, register: a.recipient.name)
  - else
    = t('no_information_available')