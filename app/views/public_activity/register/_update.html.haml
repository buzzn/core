- single_content_for :action do
  - if a.trackable
    = t("has_updated_the_register", register_name: link_to(a.trackable.name, a.trackable)).html_safe
  - else
    = t('has_updated') + ' ' + t('a_register_which_has_been_removed')