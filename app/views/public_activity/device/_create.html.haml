- single_content_for :action do
  - if a.recipient
    = t('has_created') + ' ' + t('the_device')
    %a.btn-link.text-semibold{:href => device_path(a.trackable)}
      =a.recipient.name
  - else
    = t('has_created') + ' ' + t('a_device_which_has_been_removed')