en:
  errors:
    phone_number?: "must be a valid phone-number"

    iban?: "must be a valid iban"

    email?: "must be a valid email"

    url?: "must be a valid URL"

    has_owner_role?: "missing GROUP_OWNER role"

    metering_point_id?: 'missing metering_point_id'

    grid_consumption_group?: 'BUG: missing or wrong register.meter.group'
    grid_feeding_group?: 'BUG: missing or wrong register.meter.group'
    grid_register_no_group?: 'BUG: grid register can not belong to group'

    distribution_system_operator?: 'wrong market function of distribution_system_operator'
    transmission_system_operator?: 'wrong market function of transmission_system_operator'
    electricity_supplier?: 'wrong market function of electricity_supplier'
    at_most_one_register_with_same_label?: 'must not have more than register with this label'

    localpool_owner?: 'must be the localpool owner'
    match_localpool?: 'meter.group must match contract.localpool'
    localpool_tariffs?: 'all tariff.group must match contract.localpool'